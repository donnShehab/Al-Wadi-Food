class DatabaseService {
  // هنا ممكن نضيف shared_preferences أو sqlite أو caching
  // لكن الآن نخليه جاهز للتوسّع

  Future<void> clearLocalData() async {
    // قاعدة بيانات محلية لو احتجتها مستقبلاً
    return;
  }
}
